# mbsyncrc based on
# http://www.ict4g.net/adolfo/notes/2014/12/27/EmacsIMAP.html
# ACCOUNT INFORMATION
IMAPAccount personal-gmail
# Address to connect to
Host imap.gmail.com
User mateus.harrington@gmail.com
PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.doom.d/mu4e/.mbsyncpass-personal.gpg"
AuthMechs LOGIN
SSLType IMAPS
SSLVersions TLSv1.2
CertificateFile /etc/ssl/certs/ca-certificates.crt

# THEN WE SPECIFY THE LOCAL AND REMOTE STORAGE
# - THE REMOTE STORAGE IS WHERE WE GET THE MAIL FROM (E.G., THE
#   SPECIFICATION OF AN IMAP ACCOUNT)
# - THE LOCAL STORAGE IS WHERE WE STORE THE EMAIL ON OUR COMPUTER

# REMOTE STORAGE (USE THE IMAP ACCOUNT SPECIFIED ABOVE)
IMAPStore personal-gmail-remote
Account personal-gmail

# LOCAL STORAGE (CREATE DIRECTORIES with mkdir -p ~/Maildir/personal-gmail)
MaildirStore personal-gmail-local
Path ~/Maildir/personal-gmail/
Inbox ~/Maildir/personal-gmail/INBOX
# REQUIRED ONLY IF YOU WANT TO DOWNLOAD ALL SUBFOLDERS; SYNCING SLOWS DOWN
# SubFolders Verbatim

# CONNECTIONS SPECIFY LINKS BETWEEN REMOTE AND LOCAL FOLDERS
#
# CONNECTIONS ARE SPECIFIED USING PATTERNS, WHICH MATCH REMOTE MAIl
# FOLDERS. SOME COMMONLY USED PATTERS INCLUDE:
#
# 1 "*" TO MATCH EVERYTHING
# 2 "!DIR" TO EXCLUDE "DIR"
# 3 "DIR" TO MATCH DIR

Channel personal-gmail-inbox
Master :personal-gmail-remote:
Slave :personal-gmail-local:
Patterns "INBOX"
Create Both
Expunge Both
SyncState *

Channel personal-gmail-trash
Master :personal-gmail-remote:"[Gmail]/Bin"
Slave :personal-gmail-local:"[personal].Bin"
Create Both
Expunge Both
SyncState *

Channel personal-gmail-sent
Master :personal-gmail-remote:"[Gmail]/Sent Mail"
Slave :personal-gmail-local:"[personal].Sent Mail"
Create Both
Expunge Both
SyncState *

Channel personal-gmail-all
Master :personal-gmail-remote:"[Gmail]/All Mail"
Slave :personal-gmail-local:"[personal].All Mail"
Create Both
Expunge Both
SyncState *

Channel personal-gmail-starred
Master :personal-gmail-remote:"[Gmail]/Starred"
Slave :personal-gmail-local:"[personal].Starred"
Create Both
Expunge Both
SyncState *

# GROUPS PUT TOGETHER CHANNELS, SO THAT WE CAN INVOKE
# MBSYNC ON A GROUP TO SYNC ALL CHANNELS
#
# FOR INSTANCE: "mbsync gmail" GETS MAIL FROM
# "gmail-inbox", "gmail-sent", and "gmail-trash"
#
Group personal-gmail
Channel personal-gmail-inbox
Channel personal-gmail-sent
Channel personal-gmail-trash
Channel personal-gmail-all
Channel personal-gmail-starred


# ================================================================================


# ACCOUNT INFORMATION
IMAPAccount keele-gmail
# Address to connect to
Host imap.gmail.com
User g.m.bernardo.harrington@keele.ac.uk
PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.doom.d/mu4e/.mbsyncpass-keele.gpg"
AuthMechs LOGIN
SSLType IMAPS
SSLVersions TLSv1.2
CertificateFile /etc/ssl/certs/ca-certificates.crt

# THEN WE SPECIFY THE LOCAL AND REMOTE STORAGE
# - THE REMOTE STORAGE IS WHERE WE GET THE MAIL FROM (E.G., THE
#   SPECIFICATION OF AN IMAP ACCOUNT)
# - THE LOCAL STORAGE IS WHERE WE STORE THE EMAIL ON OUR COMPUTER

# REMOTE STORAGE (USE THE IMAP ACCOUNT SPECIFIED ABOVE)
IMAPStore keele-gmail-remote
Account keele-gmail

# LOCAL STORAGE (CREATE DIRECTORIES with mkdir -p ~/Maildir/keele-gmail)
MaildirStore keele-gmail-local
Path ~/Maildir/keele-gmail/
Inbox ~/Maildir/keele-gmail/INBOX

# CONNECTIONS SPECIFY LINKS BETWEEN REMOTE AND LOCAL FOLDERS
#
# CONNECTIONS ARE SPECIFIED USING PATTERNS, WHICH MATCH REMOTE MAIl
# FOLDERS. SOME COMMONLY USED PATTERS INCLUDE:
#
# 1 "*" TO MATCH EVERYTHING
# 2 "!DIR" TO EXCLUDE "DIR"
# 3 "DIR" TO MATCH DIR

Channel keele-gmail-inbox
Master :keele-gmail-remote:
Slave :keele-gmail-local:
Patterns "INBOX"
Create Both
Expunge Both
SyncState *

Channel keele-gmail-trash
Master :keele-gmail-remote:"[Gmail]/Trash"
Slave :keele-gmail-local:"[keele].Trash"
Create Both
Expunge Both
SyncState *

Channel keele-gmail-sent
Master :keele-gmail-remote:"[Gmail]/Sent Mail"
Slave :keele-gmail-local:"[keele].Sent Mail"
Create Both
Expunge Both
SyncState *

Channel keele-gmail-all
Master :keele-gmail-remote:"[Gmail]/All Mail"
Slave :keele-gmail-local:"[keele].All Mail"
Create Both
Expunge Both
SyncState *

Channel keele-gmail-starred
Master :keele-gmail-remote:"[Gmail]/Starred"
Slave :keele-gmail-local:"[keele].Starred"
Create Both
Expunge Both
SyncState *

# GROUPS PUT TOGETHER CHANNELS, SO THAT WE CAN INVOKE
# MBSYNC ON A GROUP TO SYNC ALL CHANNELS
#
# FOR INSTANCE: "mbsync gmail" GETS MAIL FROM
# "gmail-inbox", "gmail-sent", and "gmail-trash"
#
Group keele-gmail
Channel keele-gmail-inbox
Channel keele-gmail-sent
Channel keele-gmail-trash
Channel keele-gmail-all
Channel keele-gmail-starred
